[build]
builder = "nixpacks"
buildCommand = "npm install && cd display && npm install && npm run build && cd ../admin && npm install && npm run build && cd .."

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"